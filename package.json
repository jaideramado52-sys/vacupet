{
  "name": "vacupet",
  "version": "1.0.0",
  "description": "VacuPet — carné de salud para mascotas (PWA de un solo archivo)",
  "private": true,
  "scripts": {
    "test": "node tests/run.mjs",
    "e2e": "playwright test",
    "e2e:install": "playwright install --with-deps chromium",
    "serve": "python -m http.server 8080 --bind 127.0.0.1",
    "lint": "eslint .",
    "lint:css": "stylelint \"**/*.css\"",
    "format": "prettier --write \"scripts/**/*.mjs\" \"tests/**/*.mjs\"",
    "format:check": "prettier --check \"scripts/**/*.mjs\" \"tests/**/*.mjs\"",
    "check": "npm run lint && npm test",
    "build": "bash netlify-build.sh",
    "cap:sync": "npm run build && cap sync",
    "cap:android": "npm run cap:sync && cap open android"
  },
  "devDependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@playwright/test": "^1.49.0",
    "eslint": "^9.0.0",
    "prettier": "^3.3.0",
    "stylelint": "^16.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.1",
    "@revenuecat/purchases-capacitor": "^13.2.2"
  }
}
