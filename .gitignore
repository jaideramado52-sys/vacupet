# Sistema
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/
.claude/

# Node / Playwright
node_modules/
package-lock.json
playwright-report/
test-results/
.playwright/

# Build de Netlify
dist/
.netlify/

# Capacitor: el proyecto nativo SÍ se versiona; sus builds y assets copiados NO.
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
ios/App/App/public/
ios/App/Pods/

# Firma de Android — NUNCA subir el keystore
*.jks
*.keystore

# Perfiles temporales de Chrome (pruebas headless) y archivos de trabajo
.ct*/
.shot*/
.chrome-test/
_*.html
_*.js
_*.txt

# Supabase CLI
supabase/.temp/

# Secretos de despliegue — NUNCA subir
.env.deploy
.env
*.key
*-private*.json
keys.txt

# Nota: supabase-config.js contiene SOLO la clave pública (anon/publishable),
# que es pública por diseño; es seguro versionarla. Si prefieres no subirla,
# descomenta la línea siguiente y crea el archivo en cada despliegue:
# supabase-config.js
